// Placeholder for generated Supabase types. Replace via `supabase gen types typescript --project-id <id> > types/supabase.ts`.
export type Json = string | number | boolean | null | { [key: string]: Json } | Json[];
